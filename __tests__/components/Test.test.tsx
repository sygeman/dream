/* eslint-env jest */
import { mount } from 'enzyme';
import React from 'react';

// import Test from '../../components/Test';

describe('Test', () => {
  it('renders the h1 title', () => {
    // const test = mount(<Test>123</Test>);
    // expect(test.props().children).toEqual('123');
  });
});
