{
  "sourceRoot": "apps/server-state",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/server-state",
        "outputPath": "dist/apps/server-state"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "server-state:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "server-state:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "server-state:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "server-state:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/server-state"],
      "options": {
        "jestConfig": "apps/server-state/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/server-state/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
