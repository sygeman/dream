{
  "name": "snake",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/snake/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nxext/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/snake",
        "frameworkConfigFile": "@nxext/solid/plugins/vite"
      },
      "configurations": {
        "production": {}
      }
    },
    "move-vercel": {
      "dependsOn": ["build"],
      "executor": "nx:run-commands",
      "options": {
        "command": "cp apps/snake/vercel.json apps/snake/.vercel dist/apps/snake -a"
      }
    },
    "deploy": {
      "dependsOn": ["move-vercel"],
      "executor": "nx:run-commands",
      "options": {
        "command": "cd dist/apps/snake && yarn vercel --token \"$VERCEL_TOKEN\" --yes --prod"
      }
    },
    "serve": {
      "executor": "@nxext/vite:dev",
      "options": {
        "outputPath": "dist/apps/snake",
        "frameworkConfigFile": "@nxext/solid/plugins/vite"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/snake/**/*.{ts,spec.ts,tsx}"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/snake"],
      "options": {
        "jestConfig": "apps/snake/jest.config.ts",
        "passWithNoTests": true
      }
    }
  }
}
