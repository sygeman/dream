import { Game } from "../components/game";
import { Layout } from "../components/layout";

const GamePage = () => {
  return (
    <Layout>
      <Game />
    </Layout>
  );
};

export default GamePage;
