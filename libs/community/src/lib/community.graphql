query community($name: String!) {
  community(name: $name) {
    ...CommunityFields
  }
}

query communities {
  communities {
    ...CommunityFields
  }
}

query uniqCount {
  uniqCount
}

fragment CommunityFields on Community {
  id
  name
  title
  avatar
  onlineCount
}
