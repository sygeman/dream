query clip($id: String!) {
  clip(id: $id) {
    ...ClipFields
  }
}

query clips {
  clips {
    ...ClipFields
  }
}

fragment ClipFields on Clip {
  id
  sourceUrl
  title
  thumbnail_url
  language
  broadcaster_id
  creator_id
  video_id
  game_id
  created_at
  score
  createdAt
  updatedAt
}
